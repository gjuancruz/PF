generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Movie {
  id       String @id @default(uuid()) @db.Uuid
  Title    String @db.Char(60)
  Plot     String @db.Text
  Genre    String @db.Char(60)
  Actors   String @db.Char(60)
  Language String @db.Char(50)
  Director String @db.Char(30)
  Release  String @db.Text
  Poster   String @db.Text
  Rated    String @db.Char(15)
  Type     String @db.Char(15)
  Trailer  String @db.Char(100)
  Runtime  Int
  show     Show[]
}

model Room {
  id     Int      @id
  seat   Seat[]
  show   Show[]
  ticket Ticket[]
}

model Sale {
  id     Int      @id
  userId String   @db.Uuid
  user   User     @relation(fields: [userId], references: [id])
  ticket Ticket[]
}

model Seat {
  id     Int     @id
  roomId Int
  room   Room    @relation(fields: [roomId], references: [id])
  ticket Ticket?
}

model Show {
  id      Int      @id
  roomId  Int
  movieId String   @db.Uuid
  movie   Movie    @relation(fields: [movieId], references: [id])
  room    Room     @relation(fields: [roomId], references: [id])
  ticket  Ticket[]
}

model Ticket {
  id     Int  @id
  seatId Int  @unique
  roomId Int
  saleId Int
  showId Int
  room   Room @relation(fields: [roomId], references: [id])
  sale   Sale @relation(fields: [saleId], references: [id])
  seat1  Seat @relation(fields: [seatId], references: [id])
  show   Show @relation(fields: [showId], references: [id])
}

model User {
  id       String @id @db.Uuid
  username String @db.Char(20)
  password String @db.Char(20)
  role     Int
  sale     Sale[]
}